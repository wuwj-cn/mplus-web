import { ContactHighlightDirective } from './contact-highlight.directive';

describe('ContactHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
